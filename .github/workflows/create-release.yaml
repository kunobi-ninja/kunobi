name: Create Release

on:
  push:
    branches: [main]
    paths:
      - 'releases/assets.json'
      - 'releases/assets.json.sha256'

jobs:
  create-release:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - name: Extract version and channel
        id: info
        run: |
          VERSION=$(jq -r '.version' releases/assets.json)
          CHANNEL=$(jq -r '.channel' releases/assets.json)

          if [ -z "$VERSION" ] || [ -z "$CHANNEL" ]; then
            echo "Error: ❌ version and channel are required" >&2
            exit 1
          fi

          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "channel=$CHANNEL" >> $GITHUB_OUTPUT
          echo "Version: $VERSION (channel: $CHANNEL)"

      - name: Prepare versioned files
        env:
          VERSION: ${{ steps.info.outputs.version }}
        run: |
          cp releases/assets.json "Kunobi_${VERSION}_release_assets.json"
          cp releases/assets.json.sha256 "Kunobi_${VERSION}_release_assets.json.sha256"

      - name: Create or update release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          VERSION: ${{ steps.info.outputs.version }}
          CHANNEL: ${{ steps.info.outputs.channel }}
        run: |
          PRERELEASE_FLAG=""
          if [[ "$CHANNEL" != "stable" ]]; then
            PRERELEASE_FLAG="--prerelease"
          fi

          # Delete existing release if present (allows re-releases)
          if gh release view "${VERSION}" &>/dev/null; then
            echo "🧹 Release ${VERSION} already exists, replacing..."
            gh release delete "${VERSION}" --yes --cleanup-tag
          fi

          gh release create "${VERSION}" \
            "Kunobi_${VERSION}_release_assets.json" \
            "Kunobi_${VERSION}_release_assets.json.sha256" \
            --title "Kunobi ${VERSION}" \
            --notes "Release ${VERSION} (${CHANNEL})" \
            $PRERELEASE_FLAG
